import {describe,it,expect} from "vitest";import {parseExhibitorInput} from "./input";const x={name:"Expo Co",slug:"expo-co",category:"Health",description:"A sufficiently detailed exhibitor description.",websiteUrl:"https://example.test",displayOrder:"1",isActive:"on",isFeatured:"on"};describe("exhibitor input",()=>{it("validates public fields",()=>expect(parseExhibitorInput(x)).not.toBeNull());it("rejects unsafe slugs and URLs",()=>{expect(parseExhibitorInput({...x,slug:"Bad Slug"})).toBeNull();expect(parseExhibitorInput({...x,websiteUrl:"javascript:alert(1)"})).toBeNull()})});
